User Adapter Application Node
=============================

    >>> from bda.bfg.ugm.model.users import Users
    >>> users = Users()
    >>> user = users['user0']
    >>> user
    <User object 'user0' at ...>

Check Properties.
::

    >>> props = user.properties

User object is editable.
::

    >>> props.editable
    True

Check Metadata.
::

    >>> md = user.metadata
    >>> md.title
    'User'
    
    >>> md.description
    'User'